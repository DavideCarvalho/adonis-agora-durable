// A hand-written or generated declaration file living next to its module — never an importable
// module itself. Discovery must ignore it (both the `.d.ts` exclusion and, if somehow imported,
// it declares no `default` workflow class).
export declare const unused: string;
